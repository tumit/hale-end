services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: hale_end_db
      POSTGRES_USER: hale_end_user
      POSTGRES_PASSWORD: secret
    ports:
      - "5832:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: